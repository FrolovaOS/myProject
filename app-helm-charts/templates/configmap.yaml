apiVersion: v1
kind: ConfigMap
metadata:
  name: my-config
  namespace: default
data:
  application.properties: |-
    topics.inputtopic=inputtopic
    topics.usertopic=usertopic
    topics.staticstopic=staticstopic